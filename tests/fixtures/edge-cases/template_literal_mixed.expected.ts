const result = `${__binop__(__binop__(1, "::", __binop__(2, "::", [])), "|>", head)}`;
const complex = `start ${__binop__(__binop__(a, "::", b), "|>", f)} middle ${__binop__(c, "|>", __binop__(g, "::", d))} end`;

